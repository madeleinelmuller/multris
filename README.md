# multris

A local multiplayer Tetris built with Pygame.

## Running

Install dependencies (Pygame) and run the game:

```bash
pip install pygame
python multris.py
```

Player 1 uses arrow keys to move and rotate. Player 2 uses WASD.
